export const PROJECT_NAME = Object.freeze({
  title: 'TIMER',
  subTitle: 'COUNTDOWN\nPROGRAM',
});

export const UNIT = Object.freeze({
  timeUnit: 60,
  msUnit: 1000,
  public: process.env.PUBLIC_URL,
});

export const PUBLIC_PATH = process.env.PUBLIC_URL;
